server {
    listen 80;
    server_name ohttp-gateway.jthess.com localhost;

    location / {
        include proxy_params;
        proxy_pass http://unix:/home/admin/ohttp-echo/flask_app.sock;
    }
}